; preupdateexec - CnCNet updater hook. Runs on the client BEFORE downloading files.
; [Delete] key=path  |  [Rename] old=new  |  [RenameFolder] old=new  |  [RenameAndMerge] old=new
; Empty = no file operations. Add sections here when you remove or move published files.
